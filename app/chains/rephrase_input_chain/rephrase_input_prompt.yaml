_type: prompt
input_variables:
  - chat_history
  - input
template: |
  Given the following conversation and a follow up input, rephrase the follow up \
  input to be a standalone input.

  Chat History:
  {chat_history}
  Follow Up Input: {input}
  Standalone Input: